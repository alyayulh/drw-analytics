{
  "steps": {
    "extensions": {
      "commands": [
        "install-php-extensions ctype curl dom fileinfo filter gd hash intl mbstring openssl pcre pdo pdo_mysql session tokenizer xml zip"
      ]
    }
  }
}